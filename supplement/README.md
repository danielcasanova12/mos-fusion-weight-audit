# Supplementary material

The PDF is organized by the paper's claims rather than by raw output filename:

1. scope and protocol;
2. zero-gate/positive-Shapley cases and bootstrap support instability;
3. Rashomon ranges and player-granularity sensitivity;
4. nested system-disjoint selection and limitations.

Rebuild all public tables and the PDF with:

```bash
bash scripts/reproduce_analysis.sh
```

The PDF and generated tables derive only from committed CSVs. Full
re-extraction requires the separately licensed datasets and model checkpoints.

